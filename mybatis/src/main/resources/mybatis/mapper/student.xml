<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.simpolor.mybatis.repository.StudentRepository">

    <select id="selectStudentTotalCount" resultType="int">
        SELECT count(*) 
          FROM demo
    </select>
    
    <select id="selectStudent" parameterType="int" resultType="student">
        SELECT *
          FROM student
         WHERE seq = #{seq}
    </select>

    <insert id="insertStudent" parameterType="student">
        INSERT INTO student ( name, grade, age, hobby )
             VALUES (
                      #{name}
                    , #{grade}
                    , #{age}
                    , #{hobby, typeHandler=com.simpolor.app.mybatis.type.StringSplitTypeHandler}
                    )
    </insert>

    <update id="updateStudent" parameterType="student">
        UPDATE student
           SET name = #{name}
             , grade = #{grade}
             , age = #{age}
             , hobby = #{hobby, typeHandler=io.simpolor.mybatis.mybatis.type.StringSplitTypeHandler}
         WHERE seq = #{seq}
    </update>

    <delete id="deleteStudent" parameterType="String">
        DELETE FROM student
              WHERE seq = #{seq}
    </delete>


</mapper>